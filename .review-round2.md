# 第二轮逐文件审查报告

> 时间: 2026-06-05 14:15 CST | 审查者: OpenClaw (N2920)
> 范围: 全项目逐文件深度审查（Python + CUDA + 文档）

---

## 一、审查清单

| 文件 | 行数 | 评级 | 问题数 |
|---|---|---|---|
| `lowp_fft/__init__.py` | 297 | ⚠️ | 3 |
| `lowp_fft/_autograd.py` | 95 | ✅ | 0 |
| `lowp_fft/bfp_fft.py` | 212 | ✅ | 1 |
| `lowp_fft/csrc/cufft_fp16.cu` | 259 | ⚠️ | 1 |
| `src/cuda/bfp_fft.cu` | 846 | ⚠️ | 1 |
| `src/cuda/fp8_verification.cu` | 451 | ✅ | 0 |
| `tests/test_autograd.py` | 419 | ✅ | 0 |
| `tests/test_bf16.py` | 386 | ✅ | 0 |
| `tests/test_bfp_fft.py` | 210 | ✅ | 0 |
| `tests/test_bfp_cuda.py` | 173 | ✅ | 0 |
| `README.md` | 93 | 🔴 | 1 |
| `docs/sprint-3.4-final-report.md` | 168 | ✅ | 0 |
| `DESIGN.md` | 121 | ⚪ | 1 |
| `setup.py` | 34 | 🟡 | 1 |

---

## 二、逐文件发现

### 🔴 MAJOR

| # | 文件 | 行 | 问题 |
|---|---|---|---|
| **M1** | `README.md` | 精度表 | **SQNR 数值全部虚高**：FP16 标 ~100 dB 实际 ~60 dB；BF16 标 ~70 dB 实际 ~53 dB；BFP FP8 标 ~40-45 dB 实际 ~22 dB。与 `sprint-3.4-final-report.md` 和 CSV 实测数据严重不一致 |

### 🟡 MINOR

| # | 文件 | 行 | 问题 |
|---|---|---|---|
| M2 | `__init__.py` | 215 | `fft()` docstring 声称支持 "fp32/fp16/bf16"，遗漏了已实现的 fp8 |
| M3 | `__init__.py` | 197 | `_bf16_fft_impl` fallback 使用 `except Exception` 裸捕获，应限定异常类型 |
| M4 | `bfp_fft.py` | 14 | `FP8_MIN_SUBNORMAL` 已定义但从未使用（死代码） |
| M5 | `cufft_fp16.cu` | 48-108 / 153-203 | BF16 plan cache 与 FP16 plan cache 90% 代码重复（~100 行），可模板化消除 |
| M6 | `bfp_fft.cu` | 213-446 | `bfp_fft_forward()` 与 `bfp_fft_inverse()` 高度重复（~230 行镜像代码） |
| M7 | `setup.py` | 6 | CUDA_HOME 硬编码 Windows 路径（首轮已记录） |

### ⚪ NIT

| # | 文件 | 行 | 问题 |
|---|---|---|---|
| N1 | `__init__.py` | 16-19 | CUDA DLL 路径硬编码 Windows 路径 |
| N2 | `bfp_fft.py` | 173 | 蝴蝶运算中隐式类型提升（float ↔ complex128），功能正确但不够显式 |
| N3 | `DESIGN.md` | 2.3 | "CPU SIMD 低精度 FFT" 节为空占位符，未填充 |
| N4 | `__init__.py` | 89-96 | `_fp16_fft_impl` 和 `_bf16_fft_impl` 的 norm 处理逻辑重复 |

---

## 三、✅ 通过的检查

### 代码质量
- 自动微分反向传播文档清晰（明确解释为何 backward 用 FP32）
- Plan cache 双检锁 + race-lose 正确释放 + 全量淘汰策略 ✅
- CUDA 错误处理：每个 cudaMalloc/cudaMemcpy/cudaFree 都有 CHECK_CUDA ✅
- 无 TODO/FIXME/HACK 残留，无硬编码用户路径
- 所有 cudaMalloc 都有对应的 cudaFree（经过配对审计）
- `fp8_verification.cu` 验证脚本完整，包含 device query + 3 种精度测试

### 测试质量
- `test_autograd.py`: 6 classes, 31 tests，覆盖 norm/N=1/n/dim/bf16/CPU fallback/边界 ✅
- `test_bf16.py`: 7 classes, 19 tests，`@pytest.mark.parametrize` 多 N 值 ✅
- `test_bfp_fft.py`: 正确的量化断言（0.0→0.0, 1.0→1.0, 500→448 clamp）✅
- `test_bfp_cuda.py`: 调用编译后的 EXE，解析 stdout，校验 SQNR ≥ 15 dB ✅
- CUDA 不可用时正确 `pytest.skip()`
- FP16/BF16 gradcheck 已标记 `@pytest.mark.skip` 并用独立梯度测试替代

### 文档与数据
- Bergach 2026 复现：实验 1 修正（forward FFT 而非 roundtrip），N=1024 SQNR 57.1 dB 匹配论文 56-61 dB ✅
- 6 份 CSV 数据齐全，与报告值一致
- 4 篇核心论文引用正确，交叉验证 28 处
- LAPTOP-CHANGES.md 777 行，逐次迭代记录完整

---

## 四、总结

| 维度 | 首轮 | 本轮 | 变化 |
|---|---|---|---|
| 🔴 MAJOR | 0 | 1 | README SQNR 虚高 |
| 🟡 MINOR | 0 | 6 | 新增 6 项 |
| ⚪ NIT | 4 | 4 | 累计 4 项 |

**结论**: 代码层面质量良好，核心逻辑正确。**最严重的问题是 README 精度表数值虚高 2-5 倍**，需要立即修正（会影响论文/报告引用）。其余均为 P2 或低优先级。

**待确认**: 需在笔记本 RTX 5070 Ti 上跑一次完整 `pytest` 验证 0 failure。
