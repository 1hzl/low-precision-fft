# 科研项目工作流基础设施

> 基于 low-precision-fft 项目提炼，适用于所有研究型项目

## 当前已就绪

| 能力 | 状态 |
|------|------|
| Git 版本控制 + N2920 ↔ 笔记本双向 push | ✅ |
| CC 自动审查 (auto-review → REVIEW.md) | ✅ |
| MemPalace 14,947 条历史检索 | ✅ |
| Paper PDF 提取 (pdftotext) | ✅ |

## 建议补的底层能力

### 1. 文献笔记模板

每个参考论文自动生成标准化笔记（格式如 REF-NOTE.md），包含：
- 核心贡献（一句话）
- 跟项目的关系
- 关键数据/指标
- 待验证假设

**工具**：`~/bin/paper-digest <pdf_path>` — 提取 PDF → 调用 CC/DeepSeek 生成笔记 → 存到项目

### 2. 实验日志模板

```
experiments/
├── 2026-06-01-fp16-fft-accuracy/
│   ├── README.md        ← 实验目标、配置、结论
│   ├── results.json     ← 原始数据
│   └── notes.md         ← 过程中的观测
```

### 3. 跨项目知识图谱

MemPalace 已部署。现在需要：
- 每个项目的核心决策自动写入记忆库
- 搜索时跨项目召回相关知识
- `memory-search "循环矩阵 FFT"` 能找到 pipeline 和 low-precision-fft 的所有相关讨论

### 4. 设计决策日志

DESIGN.md 不只写"要做什么"，还要写"为什么不做什么"：
```markdown
## 决策记录
| 日期 | 决策 | 理由 | 替代方案 |
|------|------|------|---------|
| 6/1 | 先做 FP16，后做 FP8 | 复杂度渐进 | 同时做（否决：人力不够） |
```

### 5. 自动化审查适配科研

当前 auto-review 只审查 diff。科研项目需要：
- 算法正确性检查
- 文献引用是否充分
- 实验设计是否合理

可在 `.claude/REVIEW-GUIDE.md` 里加科研项目的审查标准。

---

## 优先级

1. **文献笔记模板** → 现在就差一个脚本，我给你写
2. **实验日志模板** → 10 分钟创建目录结构
3. **设计决策日志** → 加到 DESIGN.md 模板里
4. **科研审查标准** → 下次 push 时自动生效
5. **跨项目知识图谱** → MemPalace 已就绪，靠持续使用

要不要我先把前两项落地？
