# 论文笔记：Block Circulant Adapter

> arXiv:2505.00582v2, 中山大学, 2025.7

## 核心贡献

提出 **BCA（Block Circulant Adapter）**，用块循环矩阵 + 1D FFT 做 LLM 参数高效微调。

## 关键数据

| 指标 | BCA | 对比 |
|------|-----|------|
| 参数量 | 少 14× | vs VeRA |
| 参数量 | 少 16× | vs LoRA |
| 计算量 | 少 32× | vs FourierFT |
| 精度 | 持平或更好 | — |

## 跟项目的关系

BCA 用 **1D FFT** 替代 FourierFT 的 **2D FFT**，大幅降低计算量。关键技术是循环矩阵 → FFT 加速。
我们的项目目标就是把这个 FFT 操作做成 **低精度（FP16/FP8）**版本，进一步压缩计算和存储。

论文已经验证了全精度 FFT 在微调场景有效——我们只需要证明显著降低精度后仍然可用。

## 技术要点

1. **循环矩阵结构**：每行是上一行的循环移位，可以用 1D FFT 对角化
2. **Block 划分**：大矩阵分成小块循环矩阵，避免单块训练发散
3. **训练启发式**：特殊初始化 + 学习率调度保证收敛
4. **合并式 Adapter**：训练后可合并回 LLM 权重，推理无额外开销

## 待验证

- FP16 精度下 FFT 误差是否影响微调收敛？
- FP8 精度下是否需要误差补偿策略？
- GPU CUDA kernel vs CPU SIMD 的性能对比
